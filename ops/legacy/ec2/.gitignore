# Terraform 상태 파일 (로컬 백엔드 사용 시 민감 정보 포함)
*.tfstate
*.tfstate.*
.terraform/
.terraform.lock.hcl

# 실제 값이 담긴 변수 파일 (민감 정보 포함 가능)
*.tfvars
!*.tfvars.example  # 예시 파일은 커밋 허용

# 플랜 파일
*.tfplan

# 크래시 로그
crash.log
crash.*.log

# 환경변수 파일
.env
